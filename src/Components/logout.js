import React from 'react';
import { withRouter } from "react-router-dom";

class Logout extends React.Component {
    //Logs the user out
    //To be called by the navbar
    
}
export default withRouter(Profile);